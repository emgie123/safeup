@model List<SafeUp.Models.SafeUpModels.File>

@{

    Layout = null;

}

<div id="partial-content" class="row">
    <div class="col-lg-12">
        <h2>Moje pliki</h2>
        <div class="panel panel-default">
            <table class="table">
                <thead>
                <tr>
                    <th>Lp.</th>
                    <th>Nazwa pliku</th>
                    <th>Data dodania</th>
                    <th>Rozmiar</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                @{int counter = 0;}
                @foreach (var file in Model)
                {
                    counter++;
                    <tr>
                        <td>@counter</td>
                        <td>@file.Name</td>
                        <td>@file.CreatedOn</td>
                        <td>@file.Size</td>
                        <td><button id="moreButton" type="button" class="btn-info" data-toggle="modal" data-target="#model_@counter">Więcej</button></td>
                    </tr>


                }
                </tbody>


            </table>

            <div class="modal fade bs-example-modal-lg" id="model_@counter" tabindex="-1" role="dialog" aria-labelledby="model_@counter" aria-hidden="true">
                <div class="modal-dialog modal-xs">
                    <div class="modal-content">
                        ...
                    </div>
                </div>
            </div>
        </div>
        @*

    <table>
        @foreach (var file in Model)
        {
            <tr>
                <td>@file.ID.ColumnyValue</td>
                <td>@file.Name.ColumnyValue</td>
                <td>@file.Owner.ColumnyValue</td>
                <td>@file.CreatedOn.ColumnyValue</td>
                <td>@file.Key.ColumnyValue</td>
                <td>@file.Path.ColumnyValue</td>
                <td>@file.Size.ColumnyValue</td>
            </tr>
        }
    </table>
    *@
        <a href="#" class="btn btn-success" type="button" style="border-radius: 0; margin: 20px;">Dodaj plik</a>


        <a href="#" class="btn btn-success" type="button" style="border-radius: 0; margin: 20px;">Udostępnij</a>

        <ul id="files-list">
            <!-- lista -->
        </ul>


    </div>

</div>
