
@{
    Layout = null;
}

<div id="navbar-left-col" class="col-lg-2" style="padding: 0">

    <ul class="sidebar-nav">
        @*<li role="presentation" class="active"><a href="#">Profil</a></li>*@
        <li role="presentation" class="active"></li>
        <li role="presentation">
            @Ajax.ActionLink("Moje pliki", "UserFiles", "MyFiles", new AjaxOptions()
                        {
                            UpdateTargetId = "content-col",
                            HttpMethod = "GET",
                            InsertionMode = InsertionMode.Replace,
                            LoadingElementId = "loading"
                        })
        </li>
        <li role="presentation">
            @Ajax.ActionLink("Grupy", "UserGroups", "MyGroups", new AjaxOptions()
                        {
                            UpdateTargetId = "content-col",
                            HttpMethod = "GET",
                            InsertionMode = InsertionMode.Replace,
                            LoadingElementId = "loading"
                        })
        </li>
        <li role="presentation">
            @Ajax.ActionLink("Współdzielone pliki", "UserSharedFiles", "MySharedFiles", new AjaxOptions()
            {
                UpdateTargetId = "content-col",
                HttpMethod = "GET",
                InsertionMode = InsertionMode.Replace,
                LoadingElementId = "loading"
            })
        </li>

        <li role="presentation">
            @Ajax.ActionLink("Zarządzanie", "UserManagement", "MyManagement", new AjaxOptions()
                        {
                            UpdateTargetId = "content-col",
                            HttpMethod = "GET",
                            InsertionMode = InsertionMode.Replace,
                            LoadingElementId = "loading"
                        })
        </li>

    </ul>


</div>